//UITabBarController

tintColor -> Color = Change the icon color to a custom value

//UITextField

placeholderLabel.textColor -> Color = Change the placeholder color

//UIView

layer.cornerRadius -> Number = Change the corner radius of a view

